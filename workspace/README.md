# Per-task working directory

Each task gets `workspace/<task-id>/` holding phase artifacts (requirements.md, analysis.md, design.md, critique.md, spec.md, review.md) and `src/` for delegated builds. Created at runtime by `/sdd`. See AGENTS.md §9.
